<h1>Listing remind_whos</h1>

<table>
  <tr>
    <th>Reminder</th>
    <th>Recipient</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @remind_whos.each do |remind_who| %>
  <tr>
    <td><%= remind_who.reminder.name %></td>
    <td><%= remind_who.recipient %></td>
    <td><%= link_to 'Show', remind_who %></td>
    <td><%= link_to 'Edit', edit_remind_who_path(remind_who) %></td>
    <td><%= link_to 'Destroy', remind_who, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Remind who', new_remind_who_path %>
