<%= semantic_form_for @remind_who do |f| %>
  <%= f.inputs do %>
    <%= f.input :reminder_id, :collection => Reminder.all, :as => :select%>
    <%= f.input :recipient %>
  <% end %>

  <%= f.actions do %>
    <%= f.action :submit, :as => :input %>
  <% end %>
<% end %>
